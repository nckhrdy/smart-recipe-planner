{
  "name": "smart-recipe-planner-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Supabase Edge Functions (Deno). The pure rule layer is unit-tested in Node via jest; the functions themselves run on Deno in production.",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.9.2"
  }
}
